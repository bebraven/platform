<% content_for :title, 'Take Attendance' %>

<%# Bootstrap in data as `props` so we can save a fetch for initial rendering %>

<%= react_component 'TakeAttendanceApplication', {
  attendanceEventSubmission: @attendance_event_submission,
  courseAttendanceEvents: @course_attendance_events,
  section: @section,
  state: @lti_launch.state,
} %>
